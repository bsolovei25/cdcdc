export interface IBlockDiagram {
    improvement: number;
    disabled: number;
    noReason: number;
}

export interface IBlockDiagramMock {
    improvement: number;
}

export interface IBlockDiagramColumn {
    plan: string;
    fact: string;
    pusher: string;
}
