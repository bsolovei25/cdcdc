import { LineChartPickerDirective } from './line-chart-picker.directive';

describe('LineChartPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new LineChartPickerDirective();
    expect(directive).toBeTruthy();
  });
});
