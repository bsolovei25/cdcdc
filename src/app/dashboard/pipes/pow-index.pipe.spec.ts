import { PowIndexPipe } from './pow-index.pipe';

describe('PowIndexPipe', () => {
    it('create an instance', () => {
        const pipe = new PowIndexPipe();
        expect(pipe).toBeTruthy();
    });
});
